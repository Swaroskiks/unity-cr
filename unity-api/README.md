# Unity Project Presentation API

Backend service for the Unity Project Presentation site.
Serves markdown content and images from the `content/` directory.

## Setup

1.  Install dependencies:
    ```bash
    pip install flask flask-cors python-dotenv
    ```

2.  Run the server:
    ```bash
    python app.py
    ```
    The API runs on port 5001.
